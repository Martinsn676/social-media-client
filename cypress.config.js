/* eslint-disable */
const { defineConfig } = require("cypress");

module.exports = defineConfig({
  e2e: {
    //on, config
    setupNodeEvents() {
      // implement node event listeners here
    },
  },
});
